nvchecker:
  regex: rle_([\d.]+).tar.gz
  source: regex
  url: https://cran.r-project.org/web/packages/rle/index.html
test: true
repository: arch4edu
update_script: bin/update-pkgver --target _pkgver
oldver: 0.10.0
